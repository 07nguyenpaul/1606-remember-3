<section>
  {{#each model as |reminder|}}
    <article class="spec-reminder-item">
        {{#link-to 'reminders.reminder' reminder}}{{reminder.title}}{{/link-to}}<br>
        {{reminder.date}}
    </article>
  {{/each}}
</section>
{{outlet}}
